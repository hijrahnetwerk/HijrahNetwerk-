// LOKAAL PLACEHOLDER-BESTAND.
// Bij een Netlify-deploy wordt dit bestand automatisch overschreven door build.js
// met de echte waarden uit je environment variables.
//
// Om dit lokaal te testen (bv. met `npx serve`), vul hieronder tijdelijk je eigen
// Supabase-projectgegevens in. Commit deze lokale waarden NIET met echte sleutels
// naar een publieke repo als je dat liever niet hebt (de anon key mag wel publiek,
// maar sommige mensen geven er de voorkeur aan dit niet in git-historie te zetten).
window.__ENV = {
  SUPABASE_URL: "VUL_HIER_JE_SUPABASE_URL_IN",
  SUPABASE_ANON_KEY: "VUL_HIER_JE_SUPABASE_ANON_KEY_IN"
};
